import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dev.mova',
  appName: 'mova',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
